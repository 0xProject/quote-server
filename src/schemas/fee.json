{
    "id": "/feeSchema",
    "properties": {
        "amount": {
            "$ref": "/wholeNumberSchema"
        },
        "feeType": {
            "type": "string",
            "pattern": "(^fixed$)|(^bps$)"
        },
        "token": {
            "$ref": "/addressSchema"
        }
    },
    "required": ["amount", "token", "feeType"],
    "type": "object"
}